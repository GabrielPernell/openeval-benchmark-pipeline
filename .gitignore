# Validation reports are regenerable logs (~60 MB, and PowerShell writes them
# as UTF-16). The converted items in output/*.json ARE the deliverable and are
# tracked so they can be handed off directly.
output/validation*

# Internal working notes: contains third-party contact details and
# in-progress assessments of other groups' releases.
openeval_context_handoff.md

# Python
__pycache__/
*.py[cod]
.venv/
